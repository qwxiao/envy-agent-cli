# 复制为 .env 并填入真实值：
#   cp .env.example .env
# .env 已被 .gitignore 忽略——凭证不要提交进仓库。

# 必填
API_KEY=sk-your-key-here

# 可选（不填则用内置默认值）：
#   PROVIDER 决定默认 base_url 与模型名，见 src/envy_agent_cli/llm/adapter.py
PROVIDER=deepseek
BASE_URL=https://api.deepseek.com
MODEL=deepseek-chat
